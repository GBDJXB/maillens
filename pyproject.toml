[project]
name = "maillens"
version = "0.1.0"
description = "Local email search engine"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.13.2",
    "sqlalchemy>=2.0.49",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
